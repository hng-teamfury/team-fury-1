function Person() {
  let bio = {
    firstName: "kenneth",
    lastName: "Nwachukwu",
    ID: "HNG-00620",
    language ="Javascript",
    email: "nwachukwuken92@gamil.com"
  }
  return `Hello World, this is ${bio.firstName} ${bio.lastName} with HNGi7 ID ${HNG - id} using ${bio.language} email: ${bio.email}`
}

Person();