const mydetails = [
    "Ajao Ataulmujeeb",
    "HNG-00260",
    "Ajaodlp@gmail.com",
    "Javascript",
]
const start = (name, id, lang, email) => {
    console.log(`Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${lang} for stage 2 task`)
};
start(mydetails[0], mydetails[1], mydetails[2], mydetails[3]);

