const app= ()=>{
    const data={
        fullName:"Massamar Mocktar Issa",
        id:"HNG-01843",
        language:"Javascript",
        email:"mocktarissa@gmail.com"
    } 
     console.log(`Hello World, this is ${data.fullName} with HNGi7 ID ${data.id} using ${data.language} for stage 2 task`)
}

app()
